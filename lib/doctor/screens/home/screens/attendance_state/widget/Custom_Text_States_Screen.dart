import 'package:flutter/material.dart';

class CustomTextStateScreen extends StatelessWidget {
  const CustomTextStateScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
