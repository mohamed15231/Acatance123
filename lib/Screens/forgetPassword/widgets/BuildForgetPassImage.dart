part of 'ForgetPasswordWidgetsImports.dart';

class BuildForgetPassImage extends StatelessWidget {
  const BuildForgetPassImage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'assets/images/forgetPass1.png',
    );
  }
}
